# Azure Developer CLI (azd) configuration file
# This file configures the Azure Developer CLI to deploy the AI Landing Zone

name: ai-landing-zone
metadata:
  template: ai-landing-zone@0.0.1-beta

# Infrastructure configuration
infra:
  provider: bicep
  path: infra
  module: main

# No services to deploy initially - this is just infrastructure
# Add your container apps and other services here later
